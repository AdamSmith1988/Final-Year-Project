@model IEnumerable<BookingsModel.EquiptmentType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EquiptmentTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EquiptmentTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EquiptmentTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EquiptmentTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EquiptmentTypeID })
        </td>
    </tr>
}

</table>
