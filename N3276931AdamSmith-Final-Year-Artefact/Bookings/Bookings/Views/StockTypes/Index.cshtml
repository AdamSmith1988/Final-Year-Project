@model IEnumerable<BookingsModel.StockType>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StockTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StockTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StockTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.StockTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StockTypeID })
        </td>
    </tr>
}

</table>
