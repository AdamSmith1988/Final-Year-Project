@model IEnumerable<BookingsModel.SystemAuthorization>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Add_Edit_Del)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AuthorzationName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Add_Edit_Del)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AuthorzationName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SystemAuthorizationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SystemAuthorizationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SystemAuthorizationID })
        </td>
    </tr>
}

</table>
